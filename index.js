function add(a, b) {
  var sum = a + b 
  return sum
}

function subtract(a, b) {
  var difference = a - b
  return difference
}

function multiply(a, b) {
  var product = a * b 
  return product
}

function divide(a, b) {
  var quotient = a / b 
  return quotient
}

function inc(n) {
  var incremented = n + 1
  return incremented
}

function dec(n) {
  var decremented = n - 1
  return decremented
}

function makeInt(n) {
  var integer = n
  return parseInt(integer,10)
}

function preserveDecimal(n) {
  var floating = n 
 
}